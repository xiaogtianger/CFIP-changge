优点：
排除网段统一写在 exclude_segments.txt 中
以后添加新网段，只需要在这个列表里加一行即可，例如添加 45. 网段只需加一行 45.
如何以后增加网段？
非常简单，只需在 EXCLUDE_SEGMENTS= 这个数组里添加一行，例如：
            "45."    # 新增 45. 网段
            "104."   # 新增 104. 网段
添加完后保存文件，提交即可。
